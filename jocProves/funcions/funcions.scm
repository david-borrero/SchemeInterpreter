(define (suma x y)
    (+ x y))

(define (producte x y)
    (* x y))

(define x (read))
(define y (read))

(display "La suma es: ")
(display (suma x y))
(newline)

(display "El producte es: ")
(display (producte x y))
(newline)